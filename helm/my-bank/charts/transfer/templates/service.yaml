apiVersion: v1
kind: Service
metadata:
  name: transfer
  labels:
    {{- include "transfer.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    {{- include "transfer.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http