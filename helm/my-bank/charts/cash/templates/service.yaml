apiVersion: v1
kind: Service
metadata:
  name: cash
  labels:
    {{- include "cash.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    {{- include "cash.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http